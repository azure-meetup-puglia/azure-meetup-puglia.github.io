name: Deploy Next.js site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: 'npm'
          cache-dependency-path: amp/package-lock.json
      - name: Install dependencies with platform-specific binaries
        run: |
          cd amp
          npm ci --include=optional
          # Force install platform-specific binaries
          npm install --force @tailwindcss/node lightningcss
      - name: Build with Next.js
        # Esegui npm run build nella directory amp
        run: |
          cd amp
          # Set environment variables for native module compatibility
          export NEXT_TELEMETRY_DISABLED=1
          export TAILWIND_DISABLE_TOUCH=true
          npm run build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./amp/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
