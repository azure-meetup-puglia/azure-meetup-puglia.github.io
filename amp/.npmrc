# Force install optional dependencies (needed for platform-specific binaries)
optional=true
# Force platform
target=linux-x64-gnu
libc=glibc
# Disable scripts to avoid binary issues during install
ignore-scripts=false
# Allow native rebuilds
rebuild-bundle=true